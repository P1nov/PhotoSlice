# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'PhotoSlice' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PhotoSlice
  
  pod 'SnapKit'
  pod 'IQKeyboardManagerSwift', '6.3.0'
  pod 'Alamofire'

  target 'PhotoSliceTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PhotoSliceUITests' do
    # Pods for testing
  end

end
